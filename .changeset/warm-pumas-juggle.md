---
'@neovici/cosmoz-treenode-navigator': minor
---

Add `variant` attribute to `cosmoz-treenode-button-view`. Defaults to `secondary`, allowing consumers to change the open button variant (e.g. to `tertiary`).
