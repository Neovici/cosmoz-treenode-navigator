name: CI
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - beta
      - master
      - 6.x.x
jobs:
  build:
    uses: Neovici/cfg/.github/workflows/forge.yml@master
    secrets: inherit
